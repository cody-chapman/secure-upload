{
  "name": "photo-upload-server",
  "version": "1.0.0",
  "description": "Upload receiver for the Photo Upload PWA. Saves images into per-employee, per-date folders and authenticates requests with a shared API key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "multer": "^2.0.0"
  }
}
