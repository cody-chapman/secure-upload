[Unit]
Description=Photo Upload Node.js Application
After=network.target

[Service]
Type=simple
User=nobody
Group=nobody
WorkingDirectory=/usr/src/app
ExecStart=/usr/bin/node server.js
Restart=on-failure
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
